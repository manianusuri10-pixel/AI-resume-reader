# Database Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=aicopilot

# JWT Secret (HMAC SHA-256 base64 encoded)
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# Comma-separated browser origins allowed to call the API directly
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional External LLM Integration (OpenAI / Gemini / Ollama)
# If left empty, the platform automatically utilizes its built-in deterministic AI heuristic and RAG vector engine!
LLM_API_KEY=
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_MODEL=gpt-4o-mini
